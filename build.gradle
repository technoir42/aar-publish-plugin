plugins {
    id "com.github.ben-manes.versions" version "0.21.0"
}

subprojects {
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}

wrapper {
    distributionType = Wrapper.DistributionType.ALL
}
