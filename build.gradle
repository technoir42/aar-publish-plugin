plugins {
    id "com.github.ben-manes.versions"
}

subprojects {
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}

wrapper {
    distributionType = Wrapper.DistributionType.ALL
}
